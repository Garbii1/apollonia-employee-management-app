# .env.example

# Connection string for the MongoDB container within Docker Compose
MONGODB_URI=mongodb://mongo:27017/apolloniaDb

# Port the Node.js application will listen on
PORT=3000

# (Optional) Connection string for running seed.js directly against an exposed MongoDB port
# MONGODB_URI_SEED=mongodb://localhost:27017/apolloniaDb